# 景区导览微信小程序 · 需求原文（来自 .docx）

请开发一个「景区导览」微信小程序，纯本地离线运行，不接后端。要求如下。﻿---﻿## 一、产品范围﻿两个页面，不多不少：﻿| 页面 | 职责 |﻿|---|---|﻿| 景区列表页 | 列出全部景区，每项显示该景区名称、景点数量、以及「已浏览 X%」的进度 |﻿| 景点浏览页 | 展示某个景区下的景点分段内容，可逐段向前/向后浏览，退出时记录进度 |﻿**不做清单**（明确不要实现，写进 README）：﻿```﻿❌ 后端接口、云开发、任何真实 HTTP 请求﻿❌ 地图、定位、相机、扫码﻿❌ 登录、授权、支付﻿❌ 云同步、账号体系、分享﻿❌ canvas 绘图（用普通视图元素实现即可）﻿```﻿全部数据内置在代码里，小程序在完全无网络的环境下必须可正常使用。﻿---﻿## 二、内置数据内容﻿在小程序目录下建一个数据模块，导出一个景区数组。每个景区包含：唯一 id、名称、简介、景点列表；每个景点包含：名称、正文。﻿**下面三个景区的内容直接照抄使用，不要改写、不要扩写、不要自行补充其它景区。**﻿### 景区一：西湖（id 建议 `xihu`）﻿简介：世界文化遗产「杭州西湖文化景观」，2011 年列入《世界遗产名录》，遗产区面积约 43.3 平方公里。其价值承载于自然山水、「两堤三岛」的景观格局、以及自南宋起就被认定为经典的系列题名景观「西湖十景」。整体呈现「三面云山一面城」的格局。﻿景点（共 5 个，顺序照此）：﻿1. **苏堤** —— 西湖两堤之一。北宋苏轼主持疏浚西湖时，将挖出的淤泥堆筑成纵贯湖面的长堤。西湖主要的人工元素「两堤三岛」，形成于九世纪到十二世纪之间对湖体的多次疏浚治理工程，苏堤是其中的代表。﻿2. **白堤** —— 西湖两堤中的另一条，位于湖的北侧，连接市区与孤山一带。与苏堤共同构成西湖水域的分隔与联系，是「两堤三岛」景观格局的组成部分。﻿3. **三潭印月** —— 又名小瀛洲，西湖三岛之一（三岛为小瀛洲、湖心亭、阮公墩）。三岛与两堤同为西湖景观格局中的人工要素，也是「西湖十景」题名景观之一。﻿4. **雷峰塔** —— 与湖对岸的保俶塔并称西湖「二塔」，二者分居湖之南北，是西湖天际线的重要构成，也是「一山二塔三岛三堤」这一空间秩序中的标志物。﻿5. **孤山** —— 西湖「一山」，位于湖北部，四面临水。历代文人在此留下大量题咏与史迹，是西湖十处内涵丰富的相关文化史迹所在地之一。﻿### 景区二：灵隐（id 建议 `lingyin`）﻿简介：位于西湖以西，背靠北高峰、面朝飞来峰的古代寺院群与山林景区。灵隐寺又名云林寺，是杭州最早的佛教寺院，始建于东晋咸和元年（公元 326 年），开山者为西印度僧人慧理。﻿景点（共 4 个，顺序照此）：﻿1. **灵隐寺** —— 又名云林寺、云林禅寺。始建于东晋咸和元年（公元 326 年），由西印度僧人慧理创建，为杭州最早的佛教寺院，历来被视为江南名刹。五代吴越王钱镠曾重兴开拓。﻿2. **飞来峰** —— 位于灵隐寺前的石灰岩山体。相传慧理初到此地，见山形奇秀、景色幽深，认为是「仙灵所隐」之处，遂在此建寺，寺名即由此而来。峰体上分布有历代石窟造像。﻿3. **北高峰** —— 灵隐寺背倚之山。灵隐寺「背靠北高峰、面朝飞来峰」的选址格局，是中国古代寺院依山就势布局的典型体现。﻿4. **理公岩** —— 相传为慧理在此修行时常年宴坐之处，因其名而称。属灵隐一带与开山传说直接相关的史迹点。﻿### 景区三：良渚（id 建议 `liangzhu`）﻿简介：世界文化遗产「良渚古城遗址」，2019 年 7 月在第 43 届世界遗产委员会会议上列入《世界遗产名录》。遗址距今约 5300 年至 4300 年，由古城核心区、水利系统、祭坛墓地和外围郊区等部分组成。﻿景点（共 4 个，顺序照此）：﻿1. **古城城址** —— 良渚古城分为三重结构。城址南北长约 1910 米、东西宽约 1770 米，总面积近 300 万平方米。良渚古城遗址整体包含古城核心区、水利系统、祭坛墓地与外围郊区，占地总面积达 100 平方公里。﻿2. **外围水利系统** —— 古城外围的大型水利工程遗址，是目前已知中国最古老的大型水利工程遗址，反映出当时具备大规模公共工程的组织与营建能力。﻿3. **祭坛与墓地** —— 遗址的重要组成部分之一。墓葬中出土大量精美玉器，被认为代表了中国史前玉文化的高峰，也是判断当时社会存在明显分化的实物依据。﻿4. **稻作遗存** —— 良渚文化晚期的稻作农业生产已相当成熟。世界遗产委员会认为，良渚古城遗址展现了一个以稻作农业为经济支撑、存在社会分化与统一信仰体系的早期区域性国家形态。﻿> 内容纪律：**不要为任何景点补写门票价格、开放时间、客流数据、交通线路、联系电话**。﻿> 这些属于会随时变化且容易出错的运营信息，不在本项目范围内。上面给出的全部是稳定的历史与景观信息，照用即可。﻿---﻿## 三、页面与交互要求﻿**景区列表页**﻿- 渲染全部景区，每一项显示：景区名称、该景区的景点数量、「已浏览 X%」﻿- 进度定义：**该景区已浏览到的景点序号 ÷ 该景区景点总数**，向下取整为百分比整数。﻿  例如西湖共 5 个景点，浏览到第 2 个时，显示「已浏览 40%」﻿- 从未浏览过的景区显示「已浏览 0%」﻿- 数据为空时（例如数据模块返回空数组），页面显示一个空状态提示，文案中必须包含「暂无」二字﻿**景点浏览页**﻿- 从列表页点击某个景区进入，需要把景区 id 通过页面参数传过去﻿- 展示该景区当前景点的名称与正文，并提供上一段 / 下一段的操作﻿- 首段时「上一段」不可用，末段时「下一段」不可用﻿- **每次切换景点时，把当前进度写入本地缓存**﻿- 从列表页再次进入同一景区时，**必须精确恢复到退出前的那一段**，不是从头开始﻿⚠️ 页面参数在小程序里取到的值都是字符串，如果用序号做参数要自行转换类型。﻿---﻿## 四、五条实现约束（照做，不要换方案）﻿**① 所有数据变更必须通过 `setData`。**﻿直接给 `this.data` 赋值不会触发视图更新——这是小程序双线程模型的机制限制：逻辑层与渲染层是两个独立环境，`setData` 是它们之间唯一的数据通道。请在代码注释里标明这一点。﻿**② 修改列表中单条数据时，使用 `setData` 的路径写法**（形如「数组名 + 下标 + 字段名」的字符串键），不要把整个数组重新传一遍。﻿原因：`setData` 的内容需要序列化后跨线程传输，全量重传一个长数组与只传一个字段，数据量可以相差几百倍，是小程序卡顿的首要来源。﻿**③ 浏览进度用小程序的同步本地缓存接口持久化**，键名自定。不要用内存变量、不要用全局变量，因为小程序被销毁后内存数据会丢失。﻿读取缓存时注意：**键不存在时返回的是空字符串，不是 undefined 也不是 null**，所以取值后必须做默认值兜底，否则后续按数组或对象使用会出错。﻿**④ 空状态用条件渲染指令控制，不要用样式隐藏。**﻿条件渲染在条件为假时会把节点整个从渲染树中移除，而样式隐藏只是让节点不可见但仍然存在——后面的验收断言会检查这一点。﻿**⑤ 控制单条列表项的渲染节点数量，每条不超过 5 个节点。**﻿列表项的节点数会随数据条数成倍放大，是长列表性能的主要变量。﻿---﻿## 五、验收方式与已知环境问题﻿请搭建一套**不依赖微信开发者工具、可在命令行一键运行**的自动化验收，技术选型：官方的小程序组件测试工具 `miniprogram-simulate` 配合 `jest`。﻿**jest 需要配置为浏览器式的 DOM 测试环境**（`jsdom`），因为该测试工具内部依赖 DOM 接口，在纯 Node 环境下会直接报「window 未定义」。﻿下面四个环境问题是这套组合的已知坑，**请直接按说明规避，不要自己试错**：﻿**坑 1 · 测试工具不提供页面构造函数。**﻿`miniprogram-simulate` 只实现了自定义组件的运行时，全局的页面构造函数（`Page`）、应用构造函数（`App`）以及获取应用实例的方法都不存在，直接加载页面 JS 文件会因为找不到 `Page` 而失败。﻿规避思路：读取页面 JS 的源码文本，在一个受控作用域里执行它，同时把 `Page` 等全局注入为你自己的函数，从而捕获页面传给 `Page` 的那个配置对象；再把该配置对象的 `data` 与各个方法，映射成测试工具能加载的组件形式（`data` 对应 `data`，事件方法对应 `methods`）。页面生命周期（如 `onLoad`）不会被自动触发，需要在测试里手工调用。﻿**坑 2 · 不要依赖清除模块缓存来重复加载。**﻿jest 有自己独立的模块注册表，通过删除 Node 的 `require` 缓存来强制重新执行模块的做法在 jest 下无效——第二次加载会直接命中缓存，注入的 `Page` 不会再被调用，表现为「找不到页面配置」。请使用上面坑 1 的源码执行方式，每次都是全新执行。﻿**坑 3 · 测试环境的本地缓存接口是空实现。**﻿该测试工具提供的 `wx` 对象上有数百个 API、签名齐全，但其中的同步缓存写入方法是空函数、读取方法恒定返回空字符串。因此**缓存相关的断言必须自己实现一个内存版的替代实现**（写入存入 Map、读取从 Map 取、不存在时返回空字符串以保持与真机一致），并在代码注释里明确标注「这是测试环境的替代实现，不是小程序的真实行为」。﻿**坑 4 · 读取节点属性的方式。**﻿测试工具返回的节点对象上没有直接的 `className` 等属性，需要通过节点下挂的真实 DOM 对象来读取类名与行内样式。直接访问节点的 `className` 会得到 undefined，容易被误判成「样式没生效」。﻿---﻿## 六、必须通过的验收断言（8 条，一条不过即视为未完成）﻿每条都要跑出**具体的数字或文本**并与期望值比对，不接受「看起来正常」这类描述。﻿| # | 断言 | 期望值 |﻿|---|---|---|﻿| 1 | 列表页渲染的景区条目数 | 恰好 3 条 |﻿| 2 | 直接修改 `this.data` 而不调用 `setData` | 渲染结果中查不到被修改的那段文字 |﻿| 3 | 进入西湖后逐段浏览到末段 | 依次渲染出的景点名称与内置数据的 5 个景点完全一致、顺序一致 |﻿| 4 | 西湖浏览到第 2 个景点后回到列表页 | 该项文案包含「已浏览 40%」 |﻿| 5 | 写入进度后重新挂载页面并从缓存恢复 | 恢复出的景点序号与退出前**严格相等**（不是相近、不是从头） |﻿| 6 | 数据为空时的空状态 | 存在包含「暂无」的节点；数据非空后，该空状态节点查询结果为 `null` |﻿| 7 | 条件渲染指令的行为 | 条件为假时对应节点查询结果为 `null`，而不是存在但被样式隐藏 |﻿| 8 | 移动端视口下的渲染产物 | 生成 390px 宽视口的预览页面，其中景区条目数、进度文案与断言 1、4 的结果一致 |﻿**第 8 条的截图（可选加分项）**：如果运行环境里有基于 Chromium 内核的浏览器（Chrome、Edge、360 极速浏览器等均可），可以用带调试端口的方式启动它，通过调试协议把预览页面截成图片存档。﻿做法上的两点提醒：一是要等页面真正加载完成再截，判断依据是文档就绪状态与地址已经变成目标地址，**用固定时长的等待极易截到空白页**；二是预览页面的宽度不要写成固定像素值，否则会只占半屏，用百分比宽度让它铺满视口。﻿
七、交付物
1. 可运行的小程序工程 · 全局配置、全局样式、两个页面（各自的结构/样式/逻辑/配置文件） · 数据模块（内置上面三个景区的全部内容）2. 自动化验收 · 测试配置 + 测试用例，一条命令即可全部运行 · 运行输出要能看到 8 条断言各自跑出的实际值3. README · 只写三件事：这个小程序是什么、目录结构、如何运行验收 · 不要写开发背景、动机、适用场景推广一类的内容
八、提交与自查要求
按「一条约束或一条断言」为粒度提交，提交信息写清对应哪一条
API 命名空间自查：微信小程序的接口全部挂在 wx 命名空间下。请在提交前检索整个代码目录，确认没有混入其它小程序平台的命名空间（如 my.、tt.、swan.）。这类混用在开发者工具里往往不报错、只是静默失效，是最难排查的一类问题。
禁止为了让断言通过而修改断言。如果发现某条断言与实现方式存在冲突，停下来说明冲突点，等确认后再动，不要自行放宽。
如果某条断言在你的环境里确实跑不起来，如实说明「跑不起来」及原因，不要写成「应该没问题」。
